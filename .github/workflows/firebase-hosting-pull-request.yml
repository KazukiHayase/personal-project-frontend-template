# TODO: NEXT_PUBLIC_RELAY_ENDPOINT, projectIdを埋める
# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

# name: Deploy to Firebase Hosting on PR
# on: pull_request
# jobs:
#   build_and_preview:
#     if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#
#       - name: Setup Node
#         uses: actions/setup-node@v3
#         with:
#           node-version: 16.x
#
#       - name: Install
#         run: yarn install --check-files --frozen-lockfile --non-interactive --prefer-offline
#
#       # https://github.com/FirebaseExtended/action-hosting-deploy/issues/239
#       - name: Install firebase and enable webframeworks
#         run: |-
#           npm install -g firebase-tools &&
#           firebase experiments:enable webframeworks
#
#       - name: Setup environment
#         run: |
#           touch .env
#           echo NEXT_PUBLIC_RELAY_ENDPOINT="" >> .env
#
#       - uses: FirebaseExtended/action-hosting-deploy@v0
#         with:
#           repoToken: '${{ secrets.GITHUB_TOKEN }}'
#           firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BATSUGAME }}'
#           projectId: ""
